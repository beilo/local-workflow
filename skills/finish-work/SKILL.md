---
name: finish-work
description: "收尾工作 - 提交前检查清单"
---

# 收尾工作 - 提交前检查清单

在提交或提交前使用本清单，确保工作完整。

**时机**：代码已写完并测试后、提交前

---

## 检查清单

### 1. 代码质量

> 使用 `before-dev` 阶段写入 `notes.md` 中记录的包管理器（如 `pnpm` / `npm` / `yarn` / `bun`）运行以下命令：

```bash
# 用项目实际的包管理器替换 <pm>
<pm> lint
<pm> type-check  # 或 <pm> run tsc --noEmit，视项目而定
<pm> test
```

- [ ] lint 零错误通过？
- [ ] type-check 无类型错误？
- [ ] 测试通过？
- [ ] 无 `console.log`（应使用 logger）？
- [ ] 无非空断言（`x!` 运算符）？
- [ ] 无 `any` 类型？

### 2. 代码与 Spec 同步

**代码-Spec 文档**：
- [ ] `.trellis/spec/backend/` 是否需要更新？
  - 新模式、新模块、新约定
- [ ] `.trellis/spec/frontend/` 是否需要更新？
  - 新组件、新 Hook、新模式
- [ ] `.trellis/spec/guides/` 是否需要更新？
  - 新跨层流程、从缺陷中学到的经验

**关键问题**：
> 「若我修了缺陷或发现了非显而易见的事，是否应写入文档，以免以后的自己（或他人）再踩坑？」

若答是 → 更新相关 code-spec 文档。

### 2.2. 任务文档与实现对账

无论本次是直接实现、`$executing-plans` 还是 `$subagent-driven-development`，都要核对：

- [ ] 当前实现是否仍符合 `prd.md` 的目标、范围与验收标准？
- [ ] 若存在 `plan.md`，实现是否仍符合计划中的执行边界？
- [ ] 若执行中发生偏差，是否已回写到 `prd.md` / `plan.md` / `notes.md`？
- [ ] `notes.md` 是否写清本次执行方式、来源文档、当前结论与下一步？

**阻塞规则**：
若代码已经偏离任务文档，而文档与 `notes.md` 仍保持旧状态，则**不得收尾**。先完成文档对账。

### 2.5. 代码-Spec 硬门槛（基础设施/跨层）

若改动触及基础设施或跨层契约，以下为阻塞项：

- [ ] Spec 内容可执行（真实签名/契约），而非仅原则性文字
- [ ] 包含文件路径 + 命令/API 名 + 载荷字段名
- [ ] 包含校验与错误矩阵
- [ ] 包含好/基准/坏情况
- [ ] 包含所需测试与断言点

**阻塞规则**：
若基础设施/跨层已改但相关 spec 仍抽象，则**不得收尾**。先更新相关 spec。

### 3. API 变更

若修改了 API 端点：

- [ ] 输入 schema 已更新？
- [ ] 输出 schema 已更新？
- [ ] API 文档已更新？
- [ ] 客户端代码已同步？

### 4. 数据库变更

若修改了数据库 schema：

- [ ] 已创建迁移文件？
- [ ] schema 文件已更新？
- [ ] 相关查询已更新？
- [ ] 种子数据已更新（如适用）？

### 5. 跨层验证

若改动跨多层：

- [ ] 数据在各层之间正确流动？
- [ ] 各边界错误处理正常？
- [ ] 各层类型一致？
- [ ] 加载状态已处理？

### 6. 手动测试

- [ ] 功能在浏览器/应用中正常？
- [ ] 已测边界情况？
- [ ] 已测错误状态？
- [ ] 刷新页面后仍正常？

### 6.5. 子代理 / 多步骤执行收口

若本次使用了 `$executing-plans` 或 `$subagent-driven-development`，额外检查：

- [ ] `notes.md` 已记录各阶段或各子代理的产出？
- [ ] 主会话已统一复核所有回报，而不是直接照单全收？
- [ ] 子代理引入的计划偏差、遗漏验证或遗留风险已被集中记录？

---

## 快速检查流程

```bash
# 1. 代码检查（用 notes.md 中记录的包管理器替换 <pm>）
<pm> lint && <pm> type-check

# 2. 查看改动
git status
git diff --name-only

# 3. 核对任务文档与 notes
sed -n '1,220p' .trellis/tasks/<task>/prd.md
sed -n '1,260p' .trellis/tasks/<task>/plan.md   # 若存在
sed -n '1,260p' .trellis/tasks/<task>/notes.md

# 4. 根据改动文件，核对上文相关项
```

---

## 常见疏漏

| 疏漏 | 后果 | 检查 |
|-----------|-------------|-------|
| 未更新 code-spec | 他人不知道改动 | 检查 .trellis/spec/ |
| Spec 仅抽象描述 | 基础设施/跨层改动易回归 | 要求签名/契约/矩阵/案例/测试 |
| 代码偏离 PRD / plan | 后续接手者误判真实状态 | 对账 `prd.md` / `plan.md` / `notes.md` |
| 未创建迁移 | schema 不同步 | 检查 db/migrations/ |
| 类型未同步 | 运行时错误 | 检查共享类型 |
| 测试未更新 | 虚假信心 | 跑完整测试套件 |
| 遗留 console.log | 生产日志嘈杂 | 搜索 console.log |

---

## 与其他命令的关系

```
开发流程：
  写代码 -> 测试 -> $finish-work -> git commit -> 更新 notes
                          |                           |
                   确保完整性                   记录最终状态与交接

调试流程：
  遇到缺陷 -> 修复 -> 将教训记入相关 spec 或 notes
```

- `$finish-work` - 检查工作完整性（本技能）
- `$executing-plans` - 单会话按文档推进，并在检查点更新 `notes.md`
- `$subagent-driven-development` - 子代理并行推进，但必须由主会话统一收口

---

## 7. 任务归档

当以下条件均满足时，提示用户运行归档命令：

- [ ] `notes.md` 已记录最终状态与提交信息？
- [ ] 代码已提交？

若均满足，将任务目录移入 archive：

> 归档后 `archive/` 能被 git 追踪但不再被 AI 会话自动扫描，有效降低上下文噪音。

## 核心原则

> **交付不仅包括代码，还包括文档、验证与知识沉淀。**

完整工作 = 代码 + 文档 + 测试 + 验证
