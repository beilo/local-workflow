# <任务标题> 实施计划

> 编写要求：每个步骤都要写清涉及文件、完成标志、验证方式。不要写 `TODO`、`待补充`、`按现有模式处理`、`补测试`、`类似上一步` 这类占位话术。

## 目标

<一句话说明这次实施要完成什么>

## 范围

- <本次包含>
- <本次不包含>

## 涉及文件

- `<文件路径>`：<用途或改动说明>
- `<文件路径>`：<用途或改动说明>

## 执行步骤

> 每个步骤至少写明涉及文件、完成标志与验证方式。若某一步风险较高，可补充停止条件或回退思路。若执行者仍需靠猜测才能继续，说明这一步还不够具体。

1. <第一步>
   - 涉及文件：`<文件路径>`
   - 完成标志：<做到什么算完成>
   - 验证方式：<如何确认本步完成>
   - 停止条件（可选）：<出现什么情况应暂停并回报>

2. <第二步>
   - 涉及文件：`<文件路径>`
   - 完成标志：<做到什么算完成>
   - 验证方式：<如何确认本步完成>
   - 停止条件（可选）：<出现什么情况应暂停并回报>

3. <第三步>
   - 涉及文件：`<文件路径>`
   - 完成标志：<做到什么算完成>
   - 验证方式：<如何确认本步完成>
   - 停止条件（可选）：<出现什么情况应暂停并回报>

## 验证点

- [ ] <验证点 1>
- [ ] <验证点 2>

## 风险与备注

- <风险 / 依赖 / 注意事项>

> 写完后，按 `writing-plans` 技能执行自审：覆盖 `prd.md`、清理占位符、核对命名 / 路径一致性，再决定执行方式。
